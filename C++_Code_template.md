```c++
#include <iostream>
#include <algorithm>
#include <vector>
#include <cstring>
#include <cmath>
#include <ctime>

#define maxn 1e7;
using namespace std;
typedef long long LL;
typedef pair<int,int> PII;

clock_t start,stop;
double runtime(){return ((double)(stop - start))/CLOCKS_PER_SEC;}

const int N = 1e6 + 10;

int main() {
    ios::sync_with_stdio(0),cin.tie(0);
    start = clock();
    stop = clock();
    
//cout << "time = " << runtime() << endl;
return 0;

}
```